{
  "_about": {
    "무엇인가": "작업자를 실제로 띄우는 CLI(에이전트 실행기)별 실행법·필수 플래그·검증법·특이사항",
    "왜 따로 두나": "축이 둘이라서다. 판이 사는 곳(호스트: Orca·Rottie)과 작업자를 띄우는 것(실행기: codex·omo·claude)은 다른 축인데, orca-conductor 안에 두면 둘이 한 덩어리가 된다. 이 파일은 호스트가 무엇이든 똑같이 쓰인다.",
    "용어": {
      "host": "Orca, Rottie — 터미널·워크트리·장부가 사는 곳",
      "agent runner": "codex, omo, claude — 작업자를 띄우는 CLI"
    },
    "모델 편성은 어디": "routing-providers.json 이 정한다. 이 파일은 '어떻게 실행하는가'만 정하고 '어느 모델인가'는 정하지 않는다.",
    "관통 규칙": "실행기는 저마다 라우터 위에 얹힌 두 번째 결정자를 갖고 있다. 2026-08-10 하루에만 codex 는 '모델 지정하지 마라'를 대화에 주입했고 omo 는 지정한 모델을 다른 것으로 갈아탔다. 그래서 실행기마다 '지정한 대로 도는지 확인하는 법'을 반드시 적는다.",
    "적는 법": "required_flags 와 quirks 에는 반드시 measured(실측 날짜)와 evidence(무엇을 보고 그렇게 판단했는지)를 함께 적는다. 근거 없는 항목은 넣지 않는다.",
    "확정일": "2026-08-10"
  },
  "_공통": {
    "터미널 주입": {
      "무엇": "호스트의 terminal send 는 여러 줄 텍스트를 줄마다 Enter 로 제출한다. 긴 카드를 통째로 밀어넣으면 여러 개의 별도 메시지가 된다.",
      "measured": "2026-08-10",
      "evidence": "A3-R4 카드 1121자를 통째 주입했더니 omo 가 'Separating and initializing five tasks' 로 인식. orca terminal send 에 붙여넣기 플래그 없음(--text/--enter/--interrupt 뿐).",
      "처방": "긴 지시는 파일에 쓰고 터미널에는 한 줄로 경로만 준다. 실행기와 무관하게 안전하다.",
      "참고": "괄호 붙여넣기 이스케이프(ESC[200~ ... ESC[201~)는 바이트로 통과하는 것을 cat -v 로 확인했다. 다만 TUI 가 그 모드를 지원해야 효과가 있어 파일 방식이 더 확실하다."
    },
    "발령 후 확인": "실행기를 무엇으로 쓰든, 발령 뒤 ps 로 실제 명령줄을 보고 의도한 플래그가 붙었는지 확인한다. 영수증이 아니라 실물이다."
  },
  "runners": [
    {
      "id": "codex",
      "binary": "codex",
      "spawn": "codex -p orca --model {model} -c model_reasoning_effort=\"{effort}\"",
      "profile": {
        "무엇": "-p <name> 은 $CODEX_HOME/<name>.config.toml 을 기본 설정 위에 덮는다(도움말 원문).",
        "왜 필요": "프로필 없이 뜨면 전역 설정의 무거운 MCP 가 전부 따라 뜬다.",
        "measured": "2026-08-10",
        "evidence": "같은 기준선·같은 모델·같은 프롬프트에서 -p orca 는 무거운 MCP 증가 0, 프로필 없음은 증가 3.",
        "주의": "프로필의 mcp_servers.<name>.enabled = false 는 mcp list 표시에 반영되지 않는다. 실질 차단은 command = \"true\" 더미가 한다. 표시만 보고 '프로필이 안 먹는다'고 판정하면 틀린다(실제로 한 번 틀렸다)."
      },
      "verify": {
        "how": "ps 로 실제 명령줄에 -p orca 와 --model 이 있는지 본다.",
        "why": "프로필이 안 붙으면 무거운 MCP 가 따라 뜬다."
      },
      "quirks": [
        {
          "date": "2026-08-10",
          "what": "대화에 developer 지시가 주입된다: 'The model catalog changed after Codex started; do not set model or reasoning_effort overrides until Codex restarts.' 이 지시를 받은 에이전트는 발령 명령에 --model 을 붙이는 것도 거부한다.",
          "출처": "codex 가 아니라 opencodex 프록시다. src/server/responses/collaboration.ts:250. 2026-08-10 오전에 'codex 바이너리 안의 문자열'이라고 적었던 것은 틀렸다 — codex 패키지 전체를 grep 하면 0건이고 opencodex 에만 있다.",
          "진짜 원인": "opencodex 의 로케일 버그다. 판정 코드는 app-server 프로세스의 시작 시각과 카탈로그 파일 mtime 을 비교하는데(app-server-processes.ts:630), 시작 시각을 execFileSync('ps',['-o','pid=,lstart=',...]) 뒤 Date.parse 로 읽는다(:493,:503). 시스템 로케일이 한국어면 ps 가 '2026년 8월 10일 월요일 12시 13분 19초'를 내고 Date.parse 는 NaN 을 낸다. 그러면 state='unknown' 으로 떨어지고, collaboration.ts:249 는 'stale' 과 'unknown' 을 똑같이 취급해 그 지시문을 낸다.",
          "measured": "2026-08-10",
          "evidence": "대조군까지 붙여 실측했다. 한국어 로케일: ps 원문 '2026년  8월 10일 월요일 12시 13분 19초' → Date.parse = NaN. LC_ALL=C: 'Mon Aug 10 12:13:19 2026' → Date.parse = 1786331599000. 그리고 ocx doctor 가 '[WARN] Could not verify whether the running Codex app-server's model catalog is current (start time or catalog unreadable)' 로 unknown 임을 확인해 준다.",
          "왜 재시작이 안 통하나": "app-server 는 PID 하나뿐이고 ChatGPT.app 재시작으로 실제로 새로 떴다(12:13:19 > 카탈로그 mtime 09:29:40). 시각 비교만 되면 fresh 였을 상황인데, 그 시각을 못 읽어서 영원히 unknown 이다. 몇 번을 재시작해도 안 풀린다.",
          "처방": "실제로 통한 것은 ocx v2 mode v1 하나다. 아래 '해결됨' 절을 그대로 따른다. 로케일 환경변수로 고치려는 시도는 통하지 않았다(같은 절의 '안 통한 것' 참고). 급하면 omo 를 쓴다 — 프록시를 안 거친다.",
          "함께 관측": "ws://127.0.0.1:10100/v1/responses 가 426 Upgrade Required 를 낸다. 같이 보이지만 이 지시문의 원인은 아니다 — 원인은 위의 로케일 판정이다. 두 개를 인과로 묶지 않는다.",
          "해결됨": {
            "date": "2026-08-10",
            "무엇을 했나": "kyle 승인으로 ocx v2 mode v1 적용. 그 안내문은 collabSurface(parsed)==='v2' 일 때만 나오므로 모든 모델을 v1 표면으로 강제하면 경로 자체가 안 탄다. 되돌리기는 ocx v2 mode default.",
            "왜 그게 걸렸나": "ocx v2 status 에 'multi_agent_mode: default — upstream model pins respected (sol/terra=v2, luna=v1)' 로 나온다. 우리 감독·작업자가 쓰는 gpt-5.6-sol 이 upstream 에서 v2 로 고정돼 있어서 걸렸다.",
            "measured": "2026-08-10",
            "evidence": "새 codex 세션에 같은 질문 — 적용 전 YES(토큰 20,593) / 적용 후 NO(토큰 18,708). 판A 감독 터미널도 재기동 후 NO.",
            "반드시 프로세스를 다시 띄워야 한다": "ocx 가 'Applies to NEW sessions; running sessions keep their pinned multi-agent version' 라고 명시한다. 여기서 말하는 세션은 codex 프로세스다 — TUI 의 /new 로 대화만 새로 만들면 여전히 YES 였다(실측). 터미널에서 /quit 로 codex 를 끝내고 같은 명령으로 다시 띄워야 NO 가 된다. 터미널은 그대로 두므로 자리(pane) 신분은 유지된다.",
            "안 통한 것": "LC_TIME=C ocx restart / env -u LC_ALL LC_TIME=C ocx restart 둘 다 소용없었다. 프록시는 서비스로 뜨면서 자기 로그인 환경을 다시 읽어 LC_ALL=ko_KR.UTF-8 가 그대로 붙는다(ps -Eww 로 확인). 셸에서 환경변수를 주는 방식으로는 못 고친다."
          }
        }
      ]
    },
    {
      "id": "omo",
      "binary": "omo",
      "설명": "omo-ai(= senpi 기반). 2026-08-10 09:47:04 설치, 5.0.0-0.beta.2.",
      "spawn": "omo --model {provider}/{model} --thinking {effort} --no-recommended-models",
      "verify": {
        "how": "--mode json 출력에서 message.provider 와 message.model 을 읽는다.",
        "why": "모델 자칭은 틀린다. 실측: 같은 실행에서 자칭은 'openai/gpt-5.6-senpi' / 'openai/gpt-5.4' 였는데 도구 기록은 둘 다 provider=openai-codex, model=gpt-5.6-sol 이었다.",
        "measured": "2026-08-10"
      },
      "required_flags": [
        {
          "flag": "--no-recommended-models",
          "why": "대화형 TUI 에서 지정한 모델을 버리고 추천 목록 1순위로 조용히 갈아탄다.",
          "measured": "2026-08-10",
          "evidence": "--model openai-codex/gpt-5.6-sol --thinking high 로 띄운 검수자가 화면에 category:unspecified-high(kimi-coding/k3:max) 로 표시됨. 그 한 건에 kimi 주간 쿼터가 95% -> 99% 로 올랐다.",
          "소스": "dist/core/extensions/builtin/recommended-models/index.js 의 RECOMMENDED_DEFAULT_MODELS 1순위가 [kimi-k3, max]. FLAG_NAME = no-recommended-models 로 등록되어 있고 omo --help 에는 안 보이지만 실제로 받아들여진다.",
          "주의": "print 모드(-p)에서는 플래그 없이도 안 바뀐다. 전환은 TUI 에서만 관측됐다. 발령은 TUI 이므로 반드시 붙인다."
        }
      ],
      "providers_확인됨": {
        "kimi-coding": "기본값(k3). 2026-08-10 기준 주간 99% 소진이라 사실상 못 쓴다.",
        "openai-codex": "gpt-5.6-sol / luna / terra / 5.5 / 5.4 등. ChatGPT 구독 인증."
      },
      "자체_오케스트레이션": {
        "결론": "카드만 줘도 서브에이전트를 스스로 띄운다. 스킬을 부를 필요가 없다.",
        "measured": "2026-08-10",
        "evidence": "A3-R4 세션(~/.senpi/agent/sessions/--Users-fw_m1-orca-workspaces-conductor-core-contract-1-a6-review--/2026-08-10T02-32-18-524Z_019fe983-...jsonl)에서 스킬 호출 0회인데도 task 3회(category:unspecified-high 1, subagent_type:explore 1), lsp_symbols 1 / lsp_diagnostics 2 / lsp_find_references 3, mcp__ast_grep_search 5, eval 27, read 18, bash 8, apply_patch 1. omo 플러그인 훅 omo-senpi:wake 10회, loop-guard:notice, senpi.hooks.stop-state 도 함께 찍혔다.",
        "세 갈래로 나눠 본다": {
          "자동": "서브에이전트·LSP·ast-grep·wake 훅. 카드만 줘도 발동한다.",
          "말해야 됨": "start-work / review-work / hyperplan / ulw-* 등 워크플로 스킬. SKILL.md 설명문에 'Triggers: review work, review my work, ...' 처럼 발동 낱말이 적혀 있고 모델이 그걸 보고 고른다. 카드에 그 낱말이 없으면 안 뽑는다.",
          "무조건": "ultrawork / ulw. 입력 훅이 프롬프트에 그 낱말이 있으면 지시문을 강제 주입한다(customType omo-ultrawork:directive). 모델 판단이 아니라 코드다."
        },
        "스킬 위치": "$(npm root -g)/omo-ai/plugin/skills — 21종. 사용자 스킬은 ~/.senpi/agent/skills (여기엔 orca orchestration 이 심볼릭 링크로 걸려 있다).",
        "발령 시 원칙": "실제로 쓸 방식 그대로 준다. 스킬 이름을 넣으면 'omo 가 어떤가'가 아니라 '그 스킬이 좋은가'를 재게 된다."
      },
      "시간_기록": {
        "결론": "omo 는 턴마다 걸린 시간을 스스로 찍는다. 우리가 따로 잴 필요 없다.",
        "형식": "화면 마지막 줄 — 'TPS 38.0 tok/s. Cache hit 98.9%, 101.9s'",
        "measured": "2026-08-10",
        "세션 전체": "세션 파일의 첫/마지막 timestamp 로 잰다. A3-R4 는 02:32:18 -> 02:52:34 = 20분 16초.",
        "주의": "화면의 초는 그 턴 하나이고 세션 전체가 아니다. 둘을 섞지 않는다."
      },
      "quirks": [
        {
          "date": "2026-08-10",
          "what": "codex 의 '모델 지정 금지' 지시가 없다.",
          "evidence": "같은 모델(gpt-5.6-sol)로 물었을 때 omo 는 NO, codex 는 YES."
        },
        {
          "date": "2026-08-10",
          "what": "서브에이전트는 별도 자식 프로세스다(문서: 'A senpi-task or subagent child process reads its own settings').",
          "판정": "프로세스 격리는 진짜지만 판단 독립은 아니다. 부모가 틀을 짜서 넘기므로 '다른 쪽이 자기 근거로 반대하는' 검수는 대체하지 못한다 — 이건 아직 추정이다.",
          "시험": "EXP-RI-1 (2026-08-10 발령). 구현을 1회만 하고 그 코드를 (A) 새 세션 검수자 (B) 구현한 그 세션의 자기 검수 로 나눠 검수해 발견·누락·오탐 수를 센다. 구현을 두 번 하면 결함 자체가 갈라져 비교가 안 되므로 1회로 고정했다. 발령장: conductor-core/.staging/exp-review-independence-1.md, 결과: .staging/result-EXP-RI-1.md"
        },
        {
          "date": "2026-08-10",
          "what": "첫 실행 시 changelog 화면을 띄운 뒤 프롬프트로 들어간다. 기동 확인은 프롬프트(❯)가 보일 때까지 기다린다."
        },
        {
          "date": "2026-08-10",
          "what": "확장에서 'Git is required for memory storage but was not found on PATH' 경고가 난다. 치명은 아니지만 PATH 에 git 이 없는 환경으로 뜬다는 뜻이다."
        }
      ]
    },
    {
      "id": "claude-code",
      "binary": "claude",
      "spawn": "claude --model {model} --effort {effort} --dangerously-skip-permissions",
      "verify": {
        "how": "미측정",
        "why": "2026-08-10 기준 이 실행기로는 실측하지 않았다."
      },
      "quirks": [
        {
          "date": "2026-08-10",
          "what": "미조사. codex·omo 에서 나온 '라우터 위의 두 번째 결정자'가 여기에도 있는지 확인하지 않았다.",
          "다음": "쓰기 전에 verify 방법부터 정하고, 지정한 모델대로 도는지 한 번 실측한다."
        }
      ]
    }
  ]
}