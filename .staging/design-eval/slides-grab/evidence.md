# 실사용 근거

- 사용 문서: `slides-grab`, `slides-grab-plan`, `slides-grab-html`, `slides-grab-design`, `slides-grab-export`, `slides-grab-card-news`, `slides-grab-image`의 SKILL.md와 references.
- 7종 중 image는 외부 생성 대신 "이미지 없는 HTML 모드가 적합"하다는 라우팅 판단에 사용했다.
- outline, HTML 덱, validation, 개별 PNG, PDF를 남긴다.
- npm `slides-grab@1.5.0`의 `gitHead`와 설치 고정 커밋이 일치함을 확인했다.
